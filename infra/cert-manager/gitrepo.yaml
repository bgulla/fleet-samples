kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: cert-manager
  namespace: fleet-default
spec:
  repo: https://github.com/bgulla/bullpen.git

  # To target specific clusters, uncomment this block
  # targets:
  # - name: example
  #   clusterSelector:
  #     matchLabels:
  #       cluster: example

  paths:
  - "./infra/cert-manager/"
